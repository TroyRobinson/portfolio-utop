import React from 'react';
import '../../public/globals.css';

// This is the main Next.js App component
function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />;
}

export default MyApp;
